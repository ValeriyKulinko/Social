let initialState = {
    names: [
        { id: 1, name: 'Oleg' },
        { id: 2, name: 'Kiril' },
        { id: 3, name: 'Vika' },
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}
export default sidebarReducer;